// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  // other config options if you add them later

  images: {
    domains: ['ai.arlox.io'],
  },
};

module.exports = nextConfig;
